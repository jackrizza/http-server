// use tray_item::{TrayItem, IconSource};

fn main() {

    // let mut tray = TrayItem::new("Simple HTTP Server", IconSource::Resource("")).unwrap();

    // tray.add_label("Status : ").unwrap();

    // tray.add_menu_item("Start", || {
    //     println!("Hello!");
    // }).unwrap();

    // tray.add_menu_item("Stop", || {
    //     println!("Hello!");
    // }).unwrap();

    // let mut inner = tray.inner_mut();
    // inner.add_quit_item("Quit");
    // inner.display();

}
